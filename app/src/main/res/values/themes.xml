<resources>
    <!-- Minimal XML theme; all real theming is done in Compose (DriveSyncTheme). -->
    <style name="Theme.DriveSync" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#F8F9FF</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">#F8F9FF</item>
    </style>
</resources>
