package com.iamsubho.drivesync.presentation.theme

import androidx.compose.material3.Typography

// Roboto is the Android system default — matches the prototype (font-family: Roboto).
val DriveSyncTypography = Typography()
